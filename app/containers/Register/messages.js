/*
 * Register Messages
 *
 * This contains all the text for the Register component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  profileSaved: {
    id: 'app.containers.Register.header',
    defaultMessage: 'Profile save successfully',
  },
});
